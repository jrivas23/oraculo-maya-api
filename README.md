# API Oráculo Maya (Flask + Airtable)

Esta API devuelve el Kin y su oráculo correspondiente según la fecha enviada como parámetro.

## ✅ Cómo usar localmente:
1. Instala dependencias:
   ```bash
   pip install -r requirements.txt
